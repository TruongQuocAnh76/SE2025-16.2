{
  "name": "base-playground",
  "version": "1.0.0",
  "description": "Playground for base UI components",
  "scripts": {
    "dev": "nuxt dev --host --port 3001",
    "playground": "nuxt dev .playground --host --port 3001",
    "build": "nuxt build",
    "generate": "nuxt generate",
    "preview": "nuxt preview"
  },
  "devDependencies": {
    "nuxt": "^3.13.0",
    "@nuxt/devtools": "latest",
    "tailwindcss": "^3.4.0",
    "@tailwindcss/typography": "^0.5.13"
  }
}
